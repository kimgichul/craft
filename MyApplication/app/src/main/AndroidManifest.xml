<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <uses-library
            android:name="com.google.firebase.firestore.ktx"
            android:required="false" />
        <activity
            android:name=".Login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Login.RegisterActivity"></activity> <!-- RegisterActivity 추가 -->
        <activity android:name=".Guide.GuideActivity"></activity> <!-- GuideActivity 추가 -->
        <activity android:name=".Item.ItemActivity"></activity>  <!-- ItemActivity 추가 -->
        <activity android:name=".Post.PostActivity"></activity> <!-- PostActivity 추가 -->
        <activity android:name=".Main.Craft.cpu.CpuActivity" /> <!-- CpuActivity 추가 -->
    </application>

</manifest>